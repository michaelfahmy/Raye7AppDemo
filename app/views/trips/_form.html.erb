<%= form_for(@trip) do |f| %>
  <% if @trip.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@trip.errors.count, "error") %> prohibited this trip from being saved:</h2>

      <ul>
      <% @trip.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :driver_id %><br>
    <%= f.collection_select(:driver_id, User.all, :id, :first_name, prompt: true) %>
  </div>
  <div class="field">
    <%= f.label :source_id %><br>
    <%= f.collection_select(:source_id, Place.all, :id, :name, prompt: true) %>
  </div>
  <div class="field">
    <%= f.label :destination_id %><br>
    <%= f.collection_select(:destination_id, Place.all, :id, :name, prompt: true) %>
  </div>
  <div class="field">
    <%= f.label :departure_time %><br>
    <%= f.datetime_select :departure_time %>
  </div>
  <div class="field">
    <%= f.label :seats %><br>
    <%= f.number_field :seats %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
